{{#if isEditing}}
  <div class="final-rant right-fixed-column clearfix rants-transition">
    <div class="rants-header">
      {{log isEditing}}
      <button class="edit-button" name="Edit" {{action "editRant" controller}}>Edit  {{fa-icon "edit"}}</button>
      <h2 class="h2-header">{{this.rant.title}}</h2>
    </div>
    <div class="block-it">
      <div class="outer-image gravatar-redux">
        {{gravatar-image email=rant.user.email alt=rant.user.firstName size=200}}
      </div>
      <p class="rant-body-adjust"> {{body}}</p>
    </div>
  </div>
{{else}}
  <div class="final-rant right-fixed-column clearfix rants-transition">
    <div class="rants-header">
      {{log isEditing}}
      <button class="cancel-button" name="Edit" {{action "editCancel" controller}}>Cancel  {{fa-icon "edit"}}</button>
        <h2 class="h2-header">{{input placeholder=rant.title }}</h2>
    </div>
    <div class="block-it">
    <h2 class="h2-header">{{rant.title}}</h2>
      <div class="outer-image gravatar-redux">
        {{gravatar-image email=rant.user.email alt=rant.user.firstName size=200}}
      </div>
      {{textarea}}
      <p class="rant-body-adjust"> {{body}}</p>
    </div>
  </div>
{{/if}}
